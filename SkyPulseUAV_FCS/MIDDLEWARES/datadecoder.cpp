#include "datadecoder.h"

DataDecoder::DataDecoder(QObject *parent) : QObject(parent)
{
    // Constructor for DataDecoder, initializes with parent QObject if provided
}

void DataDecoder::decodeData(const QByteArray &raw_data)
{
    // Placeholder for a method to decode raw data received as QByteArray
}

